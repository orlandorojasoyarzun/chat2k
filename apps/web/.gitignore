# Vite genera un dist/ dentro de este paquete (cubierto por el root .gitignore)
# pero emitimos vite.config por accidente antes; por si vuelve a pasar:

vite.config.js
vite.config.js.map
vite.config.d.ts
vite.config.d.ts.map

# Local env
.env
.env.local
.env.*.local